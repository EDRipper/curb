// the canonical deployed url, referenced by layout metadata, the og image,
// robots.txt, and sitemap.xml - kept in one place so it can't drift if this
// ever moves off the default vercel subdomain.
export const SITE_URL = "https://curb-theta.vercel.app";
